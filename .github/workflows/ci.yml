name: CI - Test Suite

on:
  push:
    branches: [ "**" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest ]
        python-version: [ "3.10", "3.11", "3.12", "3.13" ]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install Linux GUI / Qt Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libegl1 \
            libgl1 \
            libxcb-cursor0 \
            libxkbcommon-x11-0 \
            libxcb-icccm4 \
            libxcb-image0 \
            libxcb-keysyms1 \
            libxcb-randr0 \
            libxcb-render-util0 \
            libxcb-shape0 \
            libxcb-xfixes0 \
            libxcb-xinerama0 \
            libxcb1 \
            libx11-xcb1 \
            libdbus-1-3

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools wheel
          pip install -r requirements.txt
          pip install flake8 pytest
          pip install -e ".[dev]"

      - name: Lint and Syntax Check (flake8)
        run: |
          python -m flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=build,dist,.git,__pycache__

      - name: Run Test Suite
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          python -m pytest

      - name: Build Wheel Artifact
        run: |
          pip wheel . --no-deps --no-build-isolation -w dist/

      - name: Inspect Wheel Artifact
        run: |
          python scripts/verify_wheel.py dist/

      - name: Install and smoke-test Wheel Package (Linux)
        if: runner.os == 'Linux'
        run: |
          python -m venv .wheel-venv
          ./.wheel-venv/bin/python -m pip install --upgrade pip
          ./.wheel-venv/bin/python -m pip install dist/*.whl
          ./.wheel-venv/bin/spectrehud --version
          ./.wheel-venv/bin/spectrehud --help

      - name: Install and smoke-test Wheel Package (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m venv .wheel-venv
          & .\.wheel-venv\Scripts\python.exe -m pip install --upgrade pip
          $wheel = (Get-Item dist/*.whl).FullName
          & .\.wheel-venv\Scripts\python.exe -m pip install $wheel
          & .\.wheel-venv\Scripts\spectrehud.exe --version
          & .\.wheel-venv\Scripts\spectrehud.exe --help

      - name: Build Standalone Windows Executable (EXE)
        if: runner.os == 'Windows' && matrix.python-version == '3.11'
        run: |
          python scripts/build_exe.py

      - name: Smoke-test Standalone Windows Executable
        if: runner.os == 'Windows' && matrix.python-version == '3.11'
        run: |
          & .\dist\SpectreHUD.exe --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & .\dist\SpectreHUD.exe --help
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload Windows Executable Artifact
        if: runner.os == 'Windows' && matrix.python-version == '3.11'
        uses: actions/upload-artifact@v4
        with:
          name: SpectreHUD-Windows-x64
          path: dist/SpectreHUD.exe
          if-no-files-found: error
